# ITシニアなんでも相談室（senior-site）開発ログ

このファイルは、作業するたびに上に追記していきます。
触ったファイルの一覧は `../../DEVLOG_auto.tsv`（`dev/DEVLOG_auto.tsv`）に自動記録されています。

---

## 2026-08-31（日）その3 — 説明文とOGPを追加（SNSでリンクを貼ったときの見え方）

### やったこと
- トップページに13本の meta を追加（`index.html`）。
  - `description` … 検索結果の説明文
  - `og:title` / `og:description` / `og:type` / `og:url` / `og:site_name` / `og:locale`
  - `og:image` … `https://it.royschannel.com/image/hero-bg.jpg`（2200×1041・瀬戸内の夕景）
    ＋ `og:image:width` / `height` / `alt`
  - `twitter:card` = `summary_large_image`
  - `viewport` … 外側に無かったので同時に追加
- **内側と外側の両方に入れている**（理由は下記）。commit は2本。
  - `3f9ff9a` … 内側のみ（★これだけでは効いていなかった）
  - `e76c358` … 外側にも追加（これで効いた）
- 変更前に `_backups/index_backup_before_ogp_20260831.html` を作成。

### わかったこと・つまずいたこと
- **★最初に間違えた。内側だけに入れて commit・push まで済ませたが、SNSには1件も効いていなかった。**
  **LINE・X・Facebook のクローラはJavaScriptを実行しない。** バンドルを展開しないので、
  内側のメタタグは見えない。生HTMLで数えたら `og:image` が **0件**だった。
  → **OGPと description は「外側」に置く。**
- **タブ名（`<title>`）とは事情が違う。** ブラウザはJSを実行するので内側でよい。
  **置き場所は「それを読むのは誰か」で決める。**
  - ブラウザが読む（タブ名・canonical）→ 内側
  - SNSのクローラが読む（OGP・description）→ **外側**
  この判断基準を `site-kit/references/05-html-editing.md` の置き場所の表に追記した。
- **内側と外側の両方に置いても重複しない。** 展開時に外側の `<head>` ごと差し替わるため、
  最終DOMには内側のぶんだけが残る（全項目1件であることを確認）。
- **`og:image` と `og:url` は絶対URLで書く。** 相対パスだとクローラが拾えない。
  （参考：`roys-channel/index.html` の `og:image` は `image/roy-chara-side.jpg` と
  **相対パスのまま**。同じ問題を抱えている可能性がある。未対応。）
- 確認は生HTMLで行う。バンドル部分を除いて数えないと「内側にあるだけ」を見落とす
  （実際に見落とした）。手順は 05-html-editing.md に載せた。
- OGP用の正方形/1.91:1 の専用画像は無いので、既存の `hero-bg.jpg` を流用した。
  比率は 2.11:1 で推奨の 1.91:1 より少し横長。SNS側で軽く上下が切られる。
- **favicon（タブのアイコン）も内側・外側とも入っていない。** `senior-site/image/` に
  正方形の画像が無い（`site-logo.png` は 900×203、`kuro/kuro.png` は 620×880）。
  足すなら正方形の画像を用意する必要がある。未対応。

### 次にやること
- favicon（`rel="icon"` / `apple-touch-icon`）… 正方形画像の用意から
- `roys-channel/index.html` の `og:image` を絶対URLに直す（相対パスのまま）
- 教材ページ・特商法ページにも OGP を入れるか検討（今回はトップのみ）
- 旧 `pages.dev` から新ドメインへの転送（ドメイン移行の2段目）。dry-run ずみ

### 動作確認
- ローカル（`http://localhost:8765/senior-site/index.html`）… 展開後のDOMで
  `description`・`og:title`・`og:image`・`viewport`・`title`・`canonical` が**すべて1件**
  （重複なし）。h1・画像16枚も正常、壊れた画像0件。
- **ライブ確認ずみ。** `curl -sL "https://it.royschannel.com/"` の生HTMLから
  バンドル部分を除いて数え、**12本のメタタグすべてが出ていることを確認**
  （＝クローラから見える状態）。
- `og:image` の実物も確認：`Content-Type: image/jpeg` / `Content-Length: 261201`
  （ローカルのファイルサイズと完全一致）。
- push から反映まで約20秒。
- **SNSでの実際のカード表示は未確認**（LINE・X に貼っての目視はしていない）。


## 2026-08-31（日）その2 — ブラウザのタブ名を設定（空だったのを直した）

### やったこと
- タブ名を「**うどん県発！ITシニアなんでも相談室**」に設定（文面はロイさんが決定）。
  短い形を選んだ理由：検索結果で切れない。Roy's Channel 側は
  `Roy's Channel｜好きなことを、好きなだけ。うどん県から。`。
- `index.html`（バンドル形式）の2箇所を変更（commit `b54260a`・push ずみ）。
  - **内側**（展開後の `<head>`）… `<link rel="canonical">` の直後に `<title>` を追加
  - **外側**の `<title>Bundled Page</title>` … 同じ文面に変更
- 変更前に `_backups/index_backup_before_title_20260831.html`（163,299バイト）を作成。

### わかったこと・つまずいたこと
- **空タブの原因は「内側に `<title>` が無かった」こと。** バンドルは展開時に `<head>` ごと
  差し替えるので、外側に `<title>Bundled Page</title>` があっても消える。結果 `document.title`
  が空文字になり、タブとブックマークにURLがそのまま出ていた。
  内側の `<head>` は `charset` → `viewport` → `canonical` の3つだけだった。
- **`<title>` は canonical と同じ「内側」に置く。** 外側に置いても展開で消える
  （→ `site-kit/references/05-html-editing.md` の置き場所の表）。
- 外側も直す理由：展開されるまでの一瞬と、JSが動かない環境のため。
- 手順は `json.loads` → 置換 → `json.dumps` → `</` を `<\/` に戻す → `io.open(newline='')` で
  書き戻し → **読み直して往復一致を検査**。差分は**2行だけ**（外側1行＋バンドル1行）。
  数十行出たら改行が壊れているのでバックアップから戻す。
- 展開後の文字数は 33,591 → 33,625（+34字＝改行1＋`<title>`7＋題名18＋`</title>`8。計算と一致）。

### 次にやること
- 内側の `<head>` には `<meta name="description">` と OGP（`og:title`・`og:description`・
  `og:image`）も**入っていない**。SNSでリンクを貼ったときの見え方に影響する。
  同じ手順で足せるので、別の機会に検討する。
- 旧 `pages.dev` から新ドメインへの転送（ドメイン移行の2段目）。
  `dev/tool/domain-migration/migrate_domain.py --stage 2 --apply` で実施。dry-run ずみ。

### 動作確認
- ローカル（`http://localhost:8765/senior-site/index.html`）… `document.title` が
  「うどん県発！ITシニアなんでも相談室」、`<title>` タグは**1つだけ**（重複なし）、
  canonical・h1・画像16枚すべて正常、壊れた画像0件。
- **ライブ確認ずみ。** `curl -sL "https://it.royschannel.com/"` で外側の `<title>` を確認。
  さらにブラウザで開いて**展開後**の `document.title` も同じ文面になっていることを確認
  （`titleTags=1`・壊れた画像0件・canonical は `it.royschannel.com`）。
- push から反映まで約30秒。


## 2026-08-31（日）— 独自ドメイン it.royschannel.com へ移行（内部URLの書き換え・公開ずみ）

### やったこと
- Pages プロジェクト `roys-senior-it` に `it.royschannel.com` をカスタムドメインとして追加
  （アクティブ・SSL有効）。サブドメインを `it.` にしたのはロイさんの裁定
  （シニアの方に口頭・電話で伝える場面があるので一番短いもの）。
- 7ファイルの旧URLを書き換え（commit `88b07ee`・push ずみ）。
  - `index.html`（**バンドル形式**・外側4箇所＋内側の canonical と Roy's Channel へのリンク）
  - `tokushoho.html`（canonical・転送先・`?src=main` の戻り先・`LIVE_HOSTS`）
  - `material/hajimete-no-sumaho-kyoshitsu.html`・`material/inaka-hitori-kasegu.html`・
    `material/nouikiiki-tebo.html`
  - `nouikiiki/index.html`・`nouikiiki-plus/index.html`
- **`material/worker.js` の未コミット差分493行は、今回もステージしていない**（意図どおり保留）。
- 書き換えは `dev/tool/domain-migration/migrate_domain.py` で実施。バンドル形式は
  書き戻したあと `json.loads` の往復一致を検査している。

### わかったこと・つまずいたこと
- **`LIVE_HOSTS` を見落とすと訪問数が0になる。** 5ファイルに `var LIVE_HOSTS = [...]` があり、
  ここに載っているホスト名でしか計測しない。**新旧の両方を入れた**
  （旧 pages.dev も開けたままなので、片方だけだと取りこぼす）。
- **`LIVE_VISIT_PAGES`（数える側）は直す必要が無かった。** 「2か所とも直す」と記録されていたので
  確認したが、中身は**パスの配列**でホスト名を含まない。新ドメインでもトップのパスは `/` のまま
  （送る側は `page: location.pathname || "/"`）。
- **旧 pages.dev → 新ドメインの転送は、あえて別の段（2段目）にした。** 1回のpushで一緒に入れると、
  独自ドメインの設定が効いていなかったときに旧URLも新URLも両方開けなくなる。
- **このファイル（DEVLOG.md）は改行コードが混在している**（LFが主・CRLFが2箇所）。
  書き足すときは主たる LF に合わせる。CRLF で足すと差分が汚れる。
- Cloudflare の画面の文言が変わっていた：「Add a domain」→「サイトを追加」→「**ドメインを接続**」。
  お名前.com も「ドメイン」ではなく「**ネームサーバー / DNS**」メニューの下だった。
- **Pages プロジェクトはドメイン画面の検索窓では出てこない。** 左上のロゴでアカウントホームに
  戻り「Workers」の「>」から。`wrangler` に `pages domain` サブコマンドは無い（4.123.0）。
- お名前.com 由来の **NSレコード22件**（各サブドメインを `*.onamae.com` に委任）がスキャンで
  見つかったので削除した。`it` の委任が残っていると `it.royschannel.com` が使えなくなる。
- 脳いきいき手帳（PWA）の保存データは `localStorage` にあり、**旧URLで入力したぶんは
  新URLでは見えない**。ロイさん承知のうえで移行（テスト公開中のため実害小）。

### 次にやること
- 旧 `pages.dev` から新ドメインへの転送を有効化（`migrate_domain.py --stage 2 --apply` → push）
- `www.royschannel.com` → apex への Redirect Rule を Cloudflare で1本
- note・YouTube固定コメントに貼った旧URLの差し替え（手作業。該当：
  `YouTube固定コメント_2026-08-23.md`・`第1クール_出す準備_2026-08-23.md`・`note放出計画.md`）
- ブラウザのタブ名が空（バンドル展開後に `<title>` が無い）。別作業として着手ずみ。

### 動作確認
- ローカル（`http://localhost:8765/senior-site/index.html`）… **バンドルは壊れていない。**
  展開後の canonical が `it.royschannel.com`、見出し「シニアの毎日を、もっとラクに、
  もっと楽しく。」も本文も正常表示、画像16枚すべて200。
  コンソールの404は `.image-slots.state.json`（バンドラーの作業用ファイル・以前から）。
- **ライブ確認ずみ。** `curl -sL` で `it.royschannel.com` のトップ・特商法・教材3ページ・
  PWA2本の計7URLすべて HTTP 200。残る `pages.dev` 1件は `LIVE_HOSTS` の意図的な旧ホスト。
- 旧 `https://roys-senior-it.pages.dev/` も HTTP 200 で開ける（2段目の前なので正常）。
- push から反映まで約15秒。


## 2026-08-19（水）— 「こんな記事・動画があります」card-6 の見出しを「暮らしの話」に変更

### やったこと
- `index.html`（バンドル形式）の `contentCards` の `card-6` の title を
  `暮らしとお金の話` → `暮らしの話` に変更（1文字列の置換のみ）。
- 変更前に `_backups/index_backup_before_card6title_20260819.html` を作成。

### わかったこと・つまずいたこと
- 05-html-editing.md の手順どおり、テンプレートJSONを json.loads → 置換 → json.dumps →
  `</` を `<\/` に戻す、で実施。CRLF維持のため `newline=''`。git diff は index.html が 1行のみ。
- `material/worker.js` にコミットされていない差分（495行）が残っている。今回の変更とは無関係。
  push するときに巻き込まないよう、index.html だけをコミットすること。
- 2026-08-18 の DEVLOG には「未push」とあるが、実際は origin/main と同期ずみ（0/0）だった。

### 次にやること
- 公開（`git commit` → `git push origin main`）はロイさんの合図待ち。
- `material/worker.js` の未コミット差分の扱いを決める。

### 動作確認
- ローカル（`http://localhost:8765/senior-site/index.html`）で、展開後のDOMに「暮らしの話」が
  表示され、「暮らしとお金の話」が消えていることをJSで確認ずみ。
- 公開ずみ（commit 9656277 → push → Cloudflare Pages 自動ビルド）。
- ライブ `https://roys-senior-it.pages.dev/` を `curl -sL` で確認し、「暮らしの話」に
  置き換わっていることを確認ずみ（反映まで約30秒）。

---

## 2026-08-18（火）— プロフィール欄の訪問数・視聴回数の表示を停止／note読者の声を削除／紹介文を拡充

### やったこと
- `index.html`（バンドル形式・428行目付近）の `getStatCards()` を、カード配列を組み立てる処理から
  `return [];` だけに変更。「③ 自己紹介・実績」セクションの訪問数／はじめてのスマホ教室の視聴回数／
  脳いきいき手帳の視聴回数の3枚のカードが画面に出なくなる。
- 同セクションの「note読者の声」の空の枠を削除（テンプレートのdivごと除去）。
- プロフィールの紹介文を拡充。元は年齢・在住地・発信スタンスのみだった1文を、
  YouTube/noteでの発信内容と、休日のバウンドテニス大会運営・バイク/キャンプ（既存の「もう一つの顔」
  セクションと同じ内容）を足して3文に。

### わかったこと・つまずいたこと
- **データの記録（保存）とこの表示は別の仕組み。** 訪問・視聴の記録は `/track-page`（line 55付近、
  外側の`<head>`）や `/track` のPOSTでWorker→KVに保存されており、`getStatCards()`はそこから
  `/page-stats`で取得した値を**画面に出すだけ**の役目。表示を止めても記録は今まで通り続く。
- 表示をやめたのは今後の管理画面用に3種類のデータ（訪問数・スマホ教室視聴回数・脳いきいき手帳視聴回数）を
  そのまま貯め続けるため（ロイさんの指示・2026-08-18）。
- ローカル `python -m http.server` で開いて確認。「③ 自己紹介・実績」の見出しと本文はそのまま表示され、
  3枚のカードとnote読者の声欄が消えていることを確認。見た目上のズレは無い。
- Browser pane のスクリーンショットが、スクロール後の位置で毎回真っ黒になる不具合に遭遇（ページ自体の
  問題ではなくツール側の描画不具合と判断）。read_page（アクセシビリティツリー）でテキスト内容を
  直接確認する形で代替した。

### 次にやること
- 管理画面を作ったら、`getStatCards()` を元の集計処理に戻す（または別画面で表示する）。
- ライブ環境（Cloudflare Pages）への反映はまだ。deployするタイミングはロイさんの合図待ち。

### 動作確認
- ローカル静的サーバ（`http://localhost:8123/index.html`）で確認。read_page で以下を確認ずみ：
  - プロフィール欄から3枚のカードとnote読者の声欄が消えている
  - 紹介文が拡充された内容で表示されている
  - 他のセクションの表示は崩れていない
  - **ライブでの確認は未実施（未push）。**

---

## 2026-08-16（日）— Worker に `/bt-apply` を追加（中身は Roy's Channel の作業）

### やったこと
- `material/worker.js` に **`/bt-apply`**（バウンドテニス教室の申込みメール送信）を追加した。
  `/contact` と同じ Resend を使い回している。**このサイト自体の見た目・中身は変えていない。**
- 使う側のページは Roy's Channel の `bt-kyoushitsu.html`。
  **くわしい経緯は `dev/site/roys-channel/DEVLOG.md` の 2026-08-16 に書いた。**

### わかったこと・つまずいたこと
- **worker.js はこのフォルダにあるが、使うのは2つのサイト両方**という状態になった。
  デプロイ先は今までどおり `dev/tool/senior-site-api` で `wrangler.cmd deploy`。
- ファイル先頭の説明書きに、**まだ作っていない `/bt-applications`（申込み一覧）と
  「D1保存・受付完了メール」が書かれている。**中身は未実装。ロイさんに確認中。

### 次にやること
- **未デプロイ。** 届け先が決まったら `wrangler.cmd deploy` する。

### 動作確認
- このサイト側の機能（`/track-page` `/page-stats` など）には手を触れていない。**未デプロイのため実物での確認なし。**

---

## 2026-08-15（土）その4 — push して公開。ライブ確認ずみ

### やったこと
- `main` に push（`6745623`）→ Cloudflare が**数秒で自動デプロイ**。新しい住所で公開された。
- ライブで確認して見つかった点を追い直し（`8147693`）。

### わかったこと・つまずいたこと
- **Cloudflare Pages は `.html` を落とした形でページを配る。**
  `/tokushoho.html` を開くと `/tokushoho` に転送される（GitHub Pages はそのままだった）。
  - → **canonical を `.html` 無しの形に直した**（4ページ）。`.html` のままだと
    「転送される先」を canonical に書いていることになり、ちぐはぐだった。
  - 訪問数の記録は影響なし。教材ページは `page:"/materials/…"` を**固定文字列で送っている**ため。
- **`window.name` の確認は新しいタブでやること。** 名札は**タブに残り続ける**ので、
  前に古い版を開いたタブで見ると、廃止したはずの `roy-senior` が残って見える。
  新しいタブで開き直したら空だった（＝正しく廃止できている）。危うく「直っていない」と誤報するところだった。

### 動作確認（ライブ・実物）
- `https://roys1105.github.io/roys-senior-it/` → **`https://roys-senior-it.pages.dev/` へ転送された**
- `tokushoho?src=main` → 「戻る」先が `https://roys-channel.pages.dev/`
- 講座ページ → Roy's Channel へのリンク3本とも新URL
- 新しいタブでの `window.name` は空

## 2026-08-15（土）その3 — 公開先を Cloudflare Pages に一本化する準備

### やったこと
- **方針**（ロイさんの裁定）：2つのサイトは**別々のサイト**として公開し、相互リンクで繋ぐ。
  公開は **Cloudflare Pages**、**GitHub は履歴の保管用**。
  - シニア相談室 … `https://roys-senior-it.pages.dev`（作成ずみ・GitHub連携あり）
  - Roy's Channel … `https://roys-channel.pages.dev`（作成ずみ・GitHub連携あり）
- **【追記】シニアのプロジェクトを `roys-senior-it-git` → `roys-senior-it` に作り直した**（ロイさん・2026-08-15）。
  **Cloudflare Pages はプロジェクト名（＝URL）をあとから変えられない**ので、作り直すしかない。
  これに伴い、`roys-senior-it-git.pages.dev` を**10ファイルすべて置換した**（HTML 8・DEVLOG 2）。
- **6ファイルを直した**（`index.html` / `tokushoho.html` / `material/` の3ページ /
  `nouikiiki/index.html` / `nouikiiki-plus/index.html`）。
  - **旧住所からの転送**を各ページの `<head>` 先頭に追加。`roys1105.github.io` で開かれたときだけ、
    `/roys-senior-it/` を取り除いて `roys-senior-it.pages.dev` の同じ場所へ飛ばす。
  - **`LIVE_HOSTS` を `["roys-senior-it.pages.dev"]` に変更**（5か所）。
    今まで `roys1105.github.io` 固定だったので、**pages.dev のアクセスが1件も数えられていなかった**。
  - **`LIVE_VISIT_PAGES` に `'/'` と `'/index.html'` を追加**（`index.html` のバンドル内）。
    トップページは `location.pathname` を記録しており、pages.dev では `/roys-senior-it/` ではなく **`/`** になる。
    **古い値も残してある**ので、これまでに溜まった数も引き続き合計される。
  - **タブの「名札」（`window.name`）を全ページで廃止**、Roy's Channel へのリンク4本から
    `target="roy-main"` を外した。行き先も `roys-channel.pages.dev` に変更。
  - **`tokushoho.html` の「どちらから来たか」判定を書き換えた。**
    別ドメインでは referrer の**パスが読めない**（ホスト名まで）ため、
    `?src=main` / `?src=senior` の印を主、**referrer のホスト名に `roys-channel` を含むか**を予備にした。
  - 各ページに `<link rel="canonical">`（新住所）を追加。

### わかったこと・つまずいたこと
- **`index.html` はバンドル形式**（1行に丸ごとエスケープされた HTML が入っている・412行目付近）。
  直すときは `\n` や `\"` を含んだ**そのままの文字列**を探して置換する。
  - **バンドルは展開時に `<head>` を差し替える。** 外側の `<head>` に置いた canonical は消える。
    → **canonical は内側（テンプレート文字列の中）に入れないと効かない**（実際に消えるのを確認して入れ直した）。
  - 転送スクリプトは**外側**でよい（展開より先に走るため、むしろ外側が正しい）。
- **1つのリポジトリが2か所に出る**（GitHub Pages と Cloudflare Pages）。
  転送も LIVE_HOSTS も「今どっちのホストか」を見て書くこと。
- **脳いきいき手帳（`nouikiiki/` `nouikiiki-plus/`）にも転送を入れた**（ロイさんの裁定・住所を一本化）。
  - 記録は `localStorage` にあり、**localStorage はドメインごとに別物**。
    旧住所で使っていた方は、転送後は**記録が空から始まる**（消えたのではなく、別の引き出しになる）。
    引き継ぐ手段は無い（ドメインをまたいで読めないため）。承知のうえでの一本化。
  - **サービスワーカーは HTML を「ネット優先」で取りに行く作り**（`sw.js` 43〜50行目）。
    そのため、**アプリを入れている人にも次にオンラインで開いた時点で転送が届く。**
    もしキャッシュ優先だったら永遠に届かなかった。ここは確認して確定した。

### 次にやること
1. ~~Cloudflare で Pages プロジェクト `roys-channel` を作る~~ → **ロイさんが作成ずみ**（GitHub連携あり）。
2. **両リポジトリを push する**（ロイさんの合図待ち）。
3. 公開後にライブで確認：転送・相互リンク往復・特商法の「戻る」先・**訪問数がまた数え始めるか**・
   脳いきいき手帳が新住所で開くか。

### 動作確認
- ローカル静的サーバで4ページを開いて確認：
  - トップ … バンドルが正常に展開、Roy's Channel ボタンは `https://roys-channel.pages.dev/`・target なし
  - `tokushoho.html?src=main` … 「戻る」先が `https://roys-channel.pages.dev/`、`window.name` は空
  - 講座ページ … Roy's Channel へのリンク3本すべて新URL・target なし、canonical 正
  - スマホ教室 … canonical 正、特商法リンクは `../tokushoho.html?src=senior` のまま（正しい）
  - 脳いきいき手帳（無料版・プラス版）… どちらも正常に起動、読み込むファイルは全部 200、canonical 正
- **ライブでの確認は未実施**（push 前）。

## 2026-08-15（土）その2

### やったこと
- **「教材ファイルはCloudflareに上げてあるはず」を調べ、上がっていないことを確認した。**
  - Cloudflareのアカウントを直接照会：**R2は未有効化**（`Please enable R2 through the Cloudflare Dashboard.` の403）、
    Worker は `roy-senior-site-api` 1つだけ（口は `/contact` `/track` `/stats` `/track-page` `/page-stats` の5つ）、
    KV は `roy-senior-site-stats` 1つだけ。**ファイルを配る場所はどこにも無い。**
  - リポジトリ側にも docx/pptx/pdf は無い（大きいファイルは `videos/kouza_ep0〜6.mp4` のみ）。
  - **記憶違いの出どころは `material/反映手順.md` の43行目**。「GitHub経由でCloudflare Pagesに自動公開」と
    書いてあったが**誤り**。実際は GitHub Pages。→ 修正し、誤りだった旨も残した。
- **配布方法は「noteの有料エリアに直接添付」で確定**（ロイさんの裁定・2026-08-15）。
- **`note教材記事_紹介のみ_下書き.md` を添付方式に直した。**
  - `★ダウンロードリンク（Word）` など3行 → `［ここに添付①〜③］` ＋実ファイル名＋添付元フォルダのパス
  - 「ダウンロードリンクが表示されます」→「3つのファイルが表示され、その場でダウンロードできます」
  - 機械検品2番に、noteのファイル添付の仕様（実物で確認したもの）を書き足した。
- **`tokushoho.html` の3か所を、note販売の実態に合わせて直した。**
  - お支払い方法：`クレジットカード決済(Square)` → `note上での決済(クレジットカード・携帯キャリア決済・PayPay・Amazon Pay・PayPal・noteポイント)`
  - 商品の引き渡し時期：`ダウンロードURLをメールにてお送りいたします` →
    `ただちにnoteの記事内に教材ファイルが表示され、その場でダウンロードいただけます`
  - 商品代金以外に必要な料金：振込手数料の話 → `商品代金以外の費用はかかりません。通信料はお客様のご負担`
  - **Square はサイト内のどこにも他に出てこない**（grep済み）。実在しない決済手段が書かれている状態だった。

### わかったこと・つまずいたこと
- **note は有料エリアにファイルを直接添付して売れる**（ヘルプセンターの実物で確認・2026-08-15）。
  - **1ファイル50MBまで**。今回の3点は 0.21MB／0.37MB／0.52MB で余裕。
  - **1日のアップロード上限は無料会員10回**（プレミアム100・pro300）。今回は3回で足りる。
  - **ダウンロードは下書きでは効かない。公開後に有効になる。** 下書きで押せなくても故障ではない。
  - 弾かれたら Zip に圧縮すれば通る、という案内あり。
  - → 外部ストレージのリンクは使わない方針で確定（リンク切れが起きないため）。
- **「Cloudflareに上げたはず」は思い違いだった。** 出どころは `反映手順.md` の誤記。
  この手の「どこに置いたか」の記憶は、**アカウントを直接照会して確かめるのが速い**（今回は3分で確定した）。
- 教材ファイルの実物は **`Desktop\RoyWorkingFolder\シニア向け教材\はじめてのスマホ教室_教材一式\`** にある。
  ここが唯一の置き場所。バックアップは無い。

- **note の販売記事が公開された。教材セットのボタンを「準備中」からリンクに戻した。**
  - URL：https://note.com/roy_s1105/n/ne6a6e71a2bfa （ロイさんから受領・2026-08-15）
  - `material/hajimete-no-sumaho-kyoshitsu.html` 852〜856行目：
    `<span class="buy-btn is-soon">ただいま準備中です</span>` ＋ その下の `<p class="soon-note">` を削除し、
    `<a class="buy-btn" href="https://note.com/roy_s1105/n/ne6a6e71a2bfa" target="_blank" rel="noopener">教材セットを見る（note）</a>` に置き換えた。
  - **CSSの `.is-soon` と `.soon-note` は残してある。** 止めたくなったら `<a>` を `<span>` に戻すだけ
    （戻し方はHTML内のコメントに書いた）。

- **デプロイした。**（commit `f3723b9` → push → `pages-build-deployment` が `success`）
  - このデプロイで出たのは下の4ファイルの変更だけ。**写真は前のセッションで既にコミット済みで、
    今日より前からライブに出ていた**（`image/cards/` の8枚とも `git ls-files` で追跡済み・HTTP 200 で実測）。
  - **【自分の誤り・戒め】いったん「写真が入っていない」と報告したが、間違いだった。**
    ブラウザで `naturalWidth` を見て6枚を「壊れている」と判定したが、**`loading="lazy"` で
    まだ読み込まれていなかっただけ**。画面外の画像は `naturalWidth` が 0 になる。
    → **判定するなら `loading` を `eager` にしてから測るか、URLに直接 curl を当てる**（今回は両方やって解消）。
  - 出したもの：`material/hajimete-no-sumaho-kyoshitsu.html`／`tokushoho.html`／`material/反映手順.md`／`DEVLOG.md`

### 配布物は「ZIP 1つ」だった（2026-08-15・ロイさんから）

- note の有料エリアに置いたのは、**Word・スライド・PDF を1つにまとめた ZIP ファイル**。
  3ファイルを別々に添付したのではない。
- これに合わせて3か所を直した：
  - `material/hajimete-no-sumaho-kyoshitsu.html` … 価格の下に
    **「3点はZIP1つ。右クリック →『すべて展開』で取り出せます（Macはダブルクリック）」**の一文を追加。
    **買った人が最初に迷うのはここ**なので、買う前のページに書いておく。
  - `tokushoho.html` … 引き渡し時期を「教材ファイル**(ZIP形式)**が表示され」に。
  - `note教材記事_紹介のみ_下書き.md` … 有料部分を ZIP 1つの案内＋**解凍のしかた**に書き換え。

- **ZIPぶんもデプロイ済み**（commit `5d2cda3` → `pages-build-deployment` は `success`）。
  ライブで両ページとも実物確認：教材ページにZIPの一文が**表示されている**（`display:block/visible`）、
  特商法の引き渡し時期も「教材ファイル(ZIP形式)が表示され…」になっている。
  - **またキャッシュに引っかかった。** デプロイ直後の1回目は古いHTMLが返り、ZIPの一文が「無い」ように見えた。
    `fetch(location.href, {cache:'reload'})` → `location.reload(true)` で解消。
    **判定は curl（生のHTML）とブラウザの両方で。** DEVLOG 2026-08-14 の「キャッシュ注意」と同じ罠。

### 特商法の所在地・電話番号を「省略方式」にした（2026-08-15・ロイさんの裁定）

**自宅住所と携帯番号を公開ページから外した。** 代わりに「ご請求をいただいた場合には、遅滞なく開示いたします。
下記のメールアドレスまでご連絡ください。」を表示（`tokushoho.html` の所在地・電話番号の2行）。

- **根拠（原文で照合・2026-08-15）**：消費者庁「通信販売広告Q&A」
  - **A17**：個人事業者でも、請求により遅滞なく提供する旨を広告に表示し、実際に遅滞なく提供できる措置を
    講じていれば、**住所・電話番号の表示を省略できる**。
  - **A18**：バーチャルオフィスやプラットフォームの住所も、3条件（連絡先として機能する合意／運営側が本人の
    現住所と本人名義の番号を把握／確実に連絡が取れる）を満たせば使える。
  - **A19**：**私書箱は不可**（営業活動の拠点ではないため）。
  - noteヘルプ「特商法の表示について」：開示請求は**note宛**に届き、note が対応する。
    「断りもなく、当社からクリエイターの個人情報を請求者に開示することはありません」と明記。
- **これは「隠せる」制度ではない。** 正当な請求が来たら開示する義務は残る。**請求が来たら遅滞なく応じること。**
- **【訂正】note に「特定商取引法に基づく表記」の設定欄は無い。**
  いったん「［アカウント設定］→［支払先・販売者情報］→［特定商取引法に基づく表記］でチェック」と案内したが、
  **これは誤り**（第三者のnote記事の記述を、公式ヘルプで確かめずに書いた）。ロイさんが実際の画面を見て気づいた。
  - 公式ヘルプが言う「掲載」は、**クリエイターが自分で記事などに書く**という意味。専用欄は用意されていない。
  - 「設定 → お支払先」の住所・氏名は**決済と振込のための情報**（Amazon Pay の審査に使う）。
    公開ページには出ないし、特商法表記でもない。
  - 開示請求は**note社の窓口**に届き、note が対応する（取引デジタルプラットフォーム法の
    「販売業者等情報開示請求書」）。クリエイターに直接は来ない。
  - **対応**：note の販売記事の**無料部分に、サイトの `tokushoho.html` へのリンクを1行置く**。
    `note教材記事_紹介のみ_下書き.md` に追記済み（「特定商取引法に基づく表記」＋生URL。
    note では Markdown のリンク記法が効かないので**生URLで書く**）。
  - **教訓**：手順を案内するときは、**公式ヘルプか実物の画面で確かめてから言う。**
    検索結果の要約は「それらしい嘘」を混ぜてくる。
- **教材ページから特商法ページへのリンクを足した**（`../tokushoho.html?src=senior`）。
  リーガル検品4.2の「販売ページから法定表示に到達できるか」に引っかかったため。
  **これまで教材ページからは到達できなかった**（トップページからしかリンクが無かった）。
- 住所・電話番号が**サイトの他の場所に残っていないこと**を全文検索で確認（0件）。

### 【完了】これで「売れる状態」になった（2026-08-15）

- ~~note の販売記事の無料部分に、特商法ページへのリンクを1行足す~~
  → **ロイさんが実施ずみ。** 記事下部にリンクがあることを実物で確認した（2026-08-15）。
- ~~note 側で購入者にどう見えるか本人が確認（ZIPが落とせるか・中身3点が揃っているか）~~
  → **ロイさんが確認ずみ。**

**サイト → note → 購入 → ZIP受け取り の一本道が、これで全部つながった。**
教材ページ・特商法ページ・note記事のどこから見ても、価格（1,980円）・中身（Word/スライド/PDF）・
渡し方（ZIP）・返品条件が一致している状態。

### 告知記事「記事は、これからすべて無料でお届けします」を公開した（2026-08-15）

- URL：https://note.com/roy_s1105/n/na6028607f05d （2026-08-15 21:16 公開）
- **サイトへのリンクに `?from=note-oshirase-free` が付いていることを実物で確認。**
  流入は D1 に `type="ref"` の行として溜まる（4ページとも `from` を送り、Worker が記録する。コードで確認ずみ）。
  → 原稿の申し送りにあった「`?from=` は記録されません」は**古い情報だったので訂正した**。

- **【決着】5月の有料化告知記事は、公開したあとロイさんが削除ずみだった**（本人確認・2026-08-15）。
  → **追記ブロックを貼る作業は不要になった。貼る先が無い。**
  → 撤回の意思表示は、**今回の告知記事の冒頭**（「5月に…とお知らせしました。その方針を変えます。」）で
    足りている。記事を消しても読んだ人の記憶は消えないので、**この一文が効いている**。
  → 手元の原稿 `Desktop\RoyWorkingFolder\note\公開\note有料化告知記事.md` の冒頭に
    **「削除ずみ・再公開しないこと」の注意書きを入れた**（「公開」フォルダにあるので誤解しやすいため）。

  以下は調査の経緯（記録）。
  RSSフィード（25件・2026-04-27〜08-15 を連続してカバー）を全部見たが、
  **「【お知らせ】５月１１日より、新しい記事を有料公開に切り替えます」に当たる記事が無い。**
  5月の投稿は 5/3・5/6・5/10・5/11・5/17・5/18 で、どれも通常記事。
  - 手元には原稿がある：`Desktop\RoyWorkingFolder\note\公開\note有料化告知記事.md`（「公開」フォルダに置かれている）
  - **RSSに無いことから「公開されていないのでは」と当たりを付け、本人確認で「消した」と判明した。**
    note の記事一覧は JS で描かれるので直接は読めないが、**RSS（`https://note.com/<ID>/rss`）なら
    25件ぶんの公開記事が確実に取れる。** 公開・非公開の確認に使える。

### カウンターを全部0にして、本人のアクセスを数えない仕組みを入れた（2026-08-15）

**D1 の `page_events` を全削除した。訪問数・教材ページ閲覧数・流入(ref)はすべて0。**
- 消す前のバックアップ：`_backups/d1_page_events_20260815.json`（gitignore対象なので手元だけ）
  中身は7行・全部 2026-08-15 のぶん。**大半は当日の動作確認ぶん**（特商法16回はほぼ全部が確認作業）。
- テーブルの形は残してあるので、次の訪問からまた数え始める。

**本人のアクセスを数えない仕組み（`?roy=1`）**
- 4ページ（`index.html` / `tokushoho.html` / スマホ教室 / 脳いきいき手帳）の記録スクリプトに、
  `LIVE_HOSTS` の判定のすぐ下へ**除外の関門**を足した。
- 使い方：**URLの末尾に `?roy=1` を付けて一度開く** → その端末の `localStorage` に `roy_owner=1` が入り、
  **以後ずっと数えない**。数えるほうに戻すのは `?roy=0`。
- **端末ごと・ブラウザごと**に必要（localStorage のため）。スマホとPCの両方で使うなら、両方で1回ずつ開く。
  シークレットウィンドウや、履歴を消したあとは効かない。
- **端末を特定する情報は一切送っていない**（IPで弾く方式にしていない。自宅IPは変わるうえ、
  仕組みが重くなるため）。

**動作確認（ライブで実測）**
1. `?roy=1` で開く → `roy_owner=1` が入る → その後ふつうに開いても**集計は空のまま**（数えられない）
2. `?roy=0` で解除 → ふつうに開くと**2件記録された**（＝他の人のアクセスはちゃんと数える）
3. 確認に使った行は削除し、**最終状態は0件**（`/page-stats` が `{"results":[],"totals":[]}`）
4. 4ページとも、ライブのHTMLに除外コードが入っていることを確認

### 次にやること
1. **ロイさん**：ふだん使う端末（PC・スマホ）で、一度ずつ
   `https://roys1105.github.io/roys-senior-it/?roy=1` を開いておく
2. note 教材記事・告知記事からサイトへの流入を D1 の `ref` で数える
   （`note-kyozai-sumaho` と `note-oshirase-free`）。**明日以降、数字が入っているか見る。**
   **まだ実売0件。最初の1件が出たら、どこから来た人が買ったかを必ず記録する。**
2. 告知記事の公開（`note告知_無料に戻します_下書き.md` 第4版）→ 第1クールへ（`dev/tool/DEVLOG.md` の現在地を参照）
3. 有料部分の解凍案内は、原稿には入れたが note 側に入れたかは未確認。
   購入者から「開けない」の声が出たら、そのとき足す（先回りしすぎない）。

### 動作確認
- ライブの教材ページを実物で確認：**教材セットは「ただいま準備中です」表示・購入ボタンもダウンロードリンクも無し**（2026-08-15）。
- Cloudflare：R2・Worker・KV をAPIで照会して確認（上記のとおり）。
- note のファイル添付仕様：ヘルプセンターのページをブラウザで開いて確認。
- 教材ファイル3点のサイズを実測（0.21MB／0.37MB／0.52MB）。
- note の販売記事を**実物で確認**（2026-08-15）。タイトル「離れて暮らす親に、スマホの使い方をやさしく教える教科書
  【本文は無料公開中】」、**1,980円**、無料部分にサイトへのリンクとセット内容3点の記載あり。**書き換え原稿どおりに公開されている。**
- **デプロイ後、ライブで実物を確認した（2026-08-15）。**
  - 教材ページ：ボタンが `<a>` になり、`href` は note の記事、`target="_blank"`。
    「準備中」の表示（`.is-soon`／`.soon-note`）は**残っていない**。
  - 特商法ページ：お支払い方法・引き渡し時期・商品代金以外に必要な料金の3行が、**書き換えたとおりに出ている**。

---

## 2026-08-15（土）

### やったこと
- **教材ページの年齢を自動計算にした。**
  `material/inaka-hitori-kasegu.html` に「61歳」と手打ちしていた2か所を、
  誕生日（1964年11月5日）から計算して表示する形に変えた。
  - 491行目「著者：ロイ（○歳・香川県三豊市在住…）」
  - 605行目「○歳、香川県三豊市在住。会社員を続けながら…」
  - 数字の部分を `<span class="roy-age">61</span>` にして、
    ページ末尾の `<script>` で `.roy-age` をまとめて書き換える方式。
  - ロジックはトップページの `getAge()` と同じ（日本時間の今日を出し、誕生日前なら1つ引く）。
  - `<span>` の中の「61」は、JavaScriptが動かなかったときのための予備の表示。

### わかったこと・つまずいたこと
- **トップページ（index.html）の自己紹介は、すでに自動計算になっていた。**
  テンプレート側が `{{ age }}歳` で、`getAge()` が値を入れている。
  ライブサイトでも「61歳」と正しく出ているのを確認済み。手を入れる必要はなかった。
- サイト全体で「歳」を検索したところ、手打ちで残っていたのはこの教材ページの2か所だけだった。
- **`dev` フォルダの外にあるHTMLは、ブラウザ枠では静的スナップショットになりJavaScriptが動かない。**
  今回は「同じコードを実ブラウザのライブサイト上で走らせて `.roy-age` を書き換えられるか」で確認した。

### 「もう一つの顔」に Roy's Channel へのボタンを追加した
- ⑤「もう一つの顔」のカード2枚の下に、赤いボタン
  **「Roy's Channel（メインサイト）へ →」** を中央に置いた（`index.html`）。
  - 飛び先：`https://roys1105.github.io/roys-channel/`
  - `target="roys-channel"` と**タブに名前を付けている**ので、同じ名前で開いたタブが
    すでにあれば、ブラウザは新しく開かずにそのタブへ切り替える。
    （`rel="noopener"` を付けるとこの仕組みが効かなくなるので、あえて付けていない）
  - ボタンの下に「別のタブで開きます。すでに開いているときは、そのタブに切り替わります。」の注記。
- Roy's Channel を GitHub Pages に公開したので、**飛び先は生きている**
  （https://roys1105.github.io/roys-channel/ ）。

### 累計の集計を直した（500行制限の宿題）
- **`/page-stats` に `totals` を足した**（`material/worker.js`）。
  `SELECT type, page, SUM(count) FROM page_events GROUP BY type, page` を追加で流し、
  `{ ok, results, totals }` を返す形にした。`results`（最新500行）は今までどおり。
- **トップページ側**は、今日ぶんを `results` から、累計を `totals` から取るように変更。
  `data.totals || data.results` と書いてあるので、**Workerが古いままでも数字は壊れない**
  （その場合はこれまでどおり results を足して代用する）。どちらを先に更新してもよい。
- **Workerの反映だけは手作業が要る。** Cloudflareの管理画面で `material/worker.js` の
  中身を貼り替えて Deploy する（手順は `material/反映手順.md`）。**まだ未反映。**
  反映するまで、累計は「最新500行ぶん」のままになる（今は35行程度なので実害なし）。

### デプロイした
- 2026-08-15、`git push origin main` でライブに反映（ビルド約60秒）。
  上げたもの：写真8枚（`image/cards/`）、教材ページの年齢の自動計算、
  Roy's Channelへのボタン、集計の直し、`material/worker.js` と `material/反映手順.md`。
- マーケの下書き `note教材記事_紹介のみ_下書き.md` は `.gitignore` に追加して**公開しなかった**
  （`note教材記事_*.md` のパターンで除外）。
- **講座の紹介カードは、消えたまま公開した**（ロイさんの「今あるものをデプロイ」の判断）。
  そのため**ライブから「田舎暮らしのまま、独りで稼ぐ教科書」の紹介欄が無くなった**。
  講座ページ自体（`material/inaka-hitori-kasegu.html`）は残っているが、
  **シニアサイト内のどこからもリンクされていない**状態。
  戻したくなったら `git show 95331fc:index.html` に元のカードが入っている。

### 教材セットのボタンを「準備中」にした
- `material/hajimete-no-sumaho-kyoshitsu.html` の「教材セットを見る（note）」ボタンは、
  **リンク先が `★NOTE_URL★` のままだった**（差し込み忘れ。押すと存在しないページに飛ぶ）。
  noteの販売ページができるまで、押せない表示に差し替えた。
  - `<a class="buy-btn" href="★NOTE_URL★" …>` → `<span class="buy-btn is-soon">ただいま準備中です</span>`
  - グレー（#D8DEE0）＋カーソルは矢印のまま。見て押せないと分かる形。
  - 下に「教材セットは、いま準備を進めています。ご用意ができましたら、このページでお知らせします。」
  - 価格（1,980円 買い切り）はそのまま残している。
- **戻し方**：HTMLに戻し方のコメントを書いてある。`<span>` の行を
  `<a class="buy-btn" href="noteのURL" target="_blank" rel="noopener">教材セットを見る（note）</a>`
  に置き換えるだけ。CSSの `.is-soon` は残しておいてよい。
- サイト全体を検索し、**同じような未設定のリンク（`★…★` や `href="#"`）は他に無い**ことを確認した。

### 【重要】ライブの確認は「キャッシュ」に注意
- 直したはずなのに直っていない、と見えたら**まずキャッシュを疑う**。
  GitHub Pages はHTMLに `Cache-Control: max-age=600` を付けるので、
  **10分間は古いページが使われる**（`curl -I` で確認できる）。
- 見分け方：`curl -s <URL> | grep <直した部分>` でライブの中身を直接見る。
  ここに新しいコードが入っていれば、サーバーは正しく、ブラウザ側のキャッシュが原因。
- 手っ取り早い回避：リンクに `?src=main` のような**印を足す**と、アドレスが変わるので
  古いキャッシュを踏まない。強制再読み込み（Ctrl+F5）でもよい。

### 特商法ページは「来た方向」で戻り先を変えるようにした
- `tokushoho.html` は **Roy's Channel とシニア相談室の両方からリンクされている**1枚のページ。
  どちらから来ても「シニアのトップ」に戻ってしまうのを直した。
- 判定は**2段構え**にした（2026-08-15 に改良）。
  1. **`?src=main`** … Roy's Channel 側のリンクに付けた印。いちばん確実で、
     アドレスが変わるぶん**古いキャッシュも踏まない**。
  2. `document.referrer`（1つ前に見ていたページ）… 印が無いときの予備。
     **2つのサイトが同じドメインなので、パスまで読める**（別ドメインだと読めない）。
- **名札のずれも直すようにした。** 来た方向がはっきりしているときは `window.name` を
  上書きする。古いキャッシュのせいでメインのタブが「シニア」と名乗ってしまっていても、
  次にメインから開いた時点で直る。
  - `/roys-channel/` から来た → 戻り先を `https://roys1105.github.io/roys-channel/` に差し替え
  - それ以外 → これまでどおり `index.html`（シニアのトップ）
  - 文言（「← トップページへ戻る」）は変えていない。どちらの場合も「来たサイトのトップ」なので。
- **タブの名札は上書きしない**。`if (!window.name)` で、名札が無いときだけ決める。
  すでに `roy-main` のタブで開いたなら、そのタブは `roy-main` のまま
  （でないと、メインのタブがシニア扱いになってしまう）。
- Roy's Channel 側のフッターの特商法リンクは `target="roy-main"` に変更。
  シニア相談室を見ているタブを置き換えないため。
- JavaScriptが動かない場合は、これまでどおりシニアのトップに戻る（安全側）。

### タブ2枚で行き来する形にした（名札方式）★これが最終形
- **講座ページ（`material/inaka-hitori-kasegu.html`）だけは名札が `roy-main`。**
  URLはシニアサイトの下にあるが、中身は Roy's Channel の講座だから。
  `roy-senior` にしていたときは、**シニア相談室を見ていたタブが講座に置き換わってしまった。**
  URLの置き場所と、どのタブで見せたいかは別の話。
- 各ページで `window.name` を設定し、リンクに `target` を付けた。
  - シニアサイトの各ページ：`window.name = "roy-senior"`
    （`index.html`・`tokushoho.html`・スマホ教室・脳いきいき手帳。**講座ページは除く**）
  - Roy's Channel へのリンク4か所に `target="roy-main"`
    （トップの「もう一つの顔」のボタン＋講座ページの3つ）
- 名札の付いたタブがあればそこへ切り替わり、無ければ新しく開く。
  **3枚目のタブは作られない。** 詳しい理由は `dev/site/roys-channel/DEVLOG.md` に書いた。
- 2つのサイトが同じ `roys1105.github.io` だから使える手。**別ドメインにすると使えなくなる。**

### （経過）Roy's Channel との行き来を「同じタブ」にした
- 「もう一つの顔」の Roy's Channel ボタンから **`target="roys-channel"` を外した**（同じタブで移動）。
  下にあった「別のタブで開きます。すでに開いているときは…」の注記も削除。
- **タブに名前を付けて使い回す方法は廃止した。** ブラウザは「そのページ自身が開いたタブ」しか
  見つけられないため、手で開いたタブがあると結局タブが増えてしまう。同じタブなら絶対に増えない。
- Roy's Channel 側も同じにしてある（詳しくは `dev/site/roys-channel/DEVLOG.md`）。

### 講座ページの「戻る」と問い合わせ先を Roy's Channel に向けた
- `material/inaka-hitori-kasegu.html` の3か所を変更。飛び先はいずれも Roy's Channel。
  - 上部「トップに戻る」→ **「Roy's Channel に戻る」**（`https://roys1105.github.io/roys-channel/`）
  - 下部「個別に相談してみる」→ Roy's Channel の問い合わせ欄（`.../roys-channel/#contact`）
  - 下部「トップページに戻る」→ **「Roy's Channel のトップページに戻る」**
- 文言も変えた。行き先が別のサイトなので、「トップ」だけだとどこへ行くのか分からないため。
- 別タブでは開かない（同じタブで移動）。「戻る」で新しいタブが増えるのは分かりにくいため。
- **この講座ページからシニアサイトへ戻るリンクは無くなった。** この講座は
  Roy's Channel の入り口から見てもらうものなので、そろえた形。
- 他の教材ページ（スマホ教室・脳いきいき手帳）は**シニアサイト向きのまま**。変えていない。

### 講座動画7本をAIボイス版に差し替えた
- 差し替え元：`Desktop\RoyWorkingFolder\02-資料・レポート\講座動画\AIボイス版\Irodori_0814`
  （`第0回_Irodori.mp4` 〜 `第6回_Irodori.mp4`。2026-08-14 作成、合計29MB）
- 差し替え先：`videos/kouza_ep0.mp4` 〜 `kouza_ep6.mp4`（ファイル名は変えていない。
  `material/inaka-hitori-kasegu.html` がこの名前で参照しているため、ページ側の修正は不要）
- 差し替え前に、新旧の中身を照合した。**7本とも H.264 1920×1080＋AAC** で形式は同じ。
  長さは新しい方が少し短い（例：第3回 146.7秒→132.3秒、第6回 137.4秒→127.9秒）。
- **元の音声の動画はgitの履歴に残っている。** 戻したいときは
  `git show 56716a3:videos/kouza_ep0.mp4 > videos/kouza_ep0.mp4` のように取り出せる。

### Workerを更新して500行問題を解消し、回数欄を全部リセットした
- **wrangler を入れて、このPCからCloudflareを直接操作できるようにした**（`npm i -g wrangler`）。
  - **PowerShellでは `wrangler` だけだと動かない**（`.ps1` の実行がポリシーで止められる）。
    **`wrangler.cmd` と `.cmd` を付けて呼ぶこと。**
  - ログインは、この環境からは通せなかった（`wrangler login` を裏で動かすと待ち受けがすぐ終わる。
    自前でOAuthを受ける小さなプログラムも、安全の仕組みに止められた）。
    **ロイさんのターミナルで `wrangler.cmd login` を実行してもらって解決。**
    認証情報は `%APPDATA%\xdg.config\.wrangler\config\default.toml` に保存されている。
- **デプロイ設定を `dev/tool/senior-site-api/wrangler.toml` に整備した。**
  - `main` は **サイト側の `material/worker.js` を指している**（コピーを2つ持たないため）。
    同フォルダにあった古いコピー（2026-07-20）は `worker_旧_..._使わない.js.bak` にリネーム。
  - **バインディングを書き忘れると本番が壊れる**ので注意。KV `STATS`（動画再生の旧集計）と
    D1 `DB` = `senior-site-analytics`（uuid `2e5cf8b0-8109-4ced-a74d-cab6c1a3b6bf`）の2つが要る。
  - `RESEND_API_KEY` はシークレットなので設定ファイルには書かない。**デプロイしても消えない**
    （デプロイ前後に `wrangler.cmd secret list` で残っていることを確認した）。
  - 今後の更新は、このフォルダで **`wrangler.cmd deploy`** の1回で済む。管理画面での貼り付けは不要。
- **`/page-stats` が `totals` を返すようになった**（10件）。500行問題は解消。
- **回数欄をすべてリセットした**（`DELETE FROM page_events`）。
  - **消す前に全記録をバックアップ**：`_backups/page_events_2026-08-15.json`
    （39行／訪問114回・教材45回／2026-07-23〜08-15）。`_backups/` はgitignoreなので手元だけに残る。
  - 確認のためにライブサイトを開くと、その訪問が1件記録されてしまうので、
    **確認のあとにもう一度DELETEして0に戻した。**

### プラス版の「合言葉をお持ちの方はこちら」も押せないようにした
- `material/nouikiiki-tebo.html` のプラス版の欄（`id="plus"`）にあったボタン。
  リンク自体は生きていた（`../nouikiiki-plus/` が開けてしまう状態）ので、
  準備が整うまで押せない表示にした。
  - `<a class="plus-open" href="../nouikiiki-plus/" …>` → `<span class="plus-open is-soon">`
  - グレー（#D8DEE0）＋カーソルは矢印のまま。文言は「合言葉をお持ちの方はこちら」のまま残した。
  - 下に「こちらは準備が整いしだい、お使いいただけるようになります。」を追加。
  - すぐ上の「近日ご案内します」（`.soon`）はそのまま。
- **サイト全体で `nouikiiki-plus/` への入口はこれ1か所だけ**だったので、
  これで**プラス版へは、どこからもたどり着けない**（URLを直接打てば開ける。非公開ではない）。
- **戻し方**：HTMLのコメントのとおり、`<span>` の行を元の `<a>` の1行に置き換えるだけ。

### 次にやること（2026-08-15 の作業おわり時点。ここから再開する）

**まず最初に：ロイさんの目での確認待ちが2つある**
1. **タブの動き**（メイン⇄シニア、講座、特商法）。ふだんのブラウザで
   「メイン→シニア→メイン→シニア」と押して、**3枚目のタブができないか**を見てほしい。
   こちらのブラウザ枠はスクリプトからのタブ生成を止めるため、そこだけ試せていない。
   → うまくいっていなければ、名札（`window.name`）の付け方から見直す。
2. **講座動画7本の音**（AIボイス版に差し替えずみ）。聞いて問題なければそのまま。

**ロイさん側の準備ができたら戻す（どちらもHTMLに戻し方のコメントあり）**
- プラス版が整ったら、「合言葉をお持ちの方はこちら」をリンクに戻す
  （`material/nouikiiki-tebo.html`）
- noteの販売ページができたら、教材セットの「準備中」ボタンをリンクに戻す
  （`material/hajimete-no-sumaho-kyoshitsu.html`）

**判断待ち**
- 講座の紹介カードをシニアサイトに戻すかどうか。**いまシニアサイト内に講座への入口は無い**
  （Roy's Channel 側には紹介欄がある）。戻すなら、いまのデザイン（横並びのカード）に合わせて作り直す。

**やってもよい細かいこと**
- 脳いきいき手帳の教材ページに「※新しいページ（別のタブ）で開きます」が残っている。
  アプリを開く説明なので今回は触っていない。そろえたければ消す。
- 講座動画の `moov` を先頭に寄せると再生の始まりが少し速くなる
  （`ffmpeg -i 入力 -c copy -movflags +faststart 出力`）。差し替え前も同じ状態だったので急がない。

### 動作確認（動画の差し替え）
- ライブの7本を全部ダウンロードし直し、**手元のファイルとハッシュが一致すること**を確認（7本とも一致）。
- `moov`（再生の始まりやすさに関わる位置）は、**元の動画も新しい動画も先頭には無い**。
  つまりこの点で悪くなってはいない。気になるようなら
  `ffmpeg -i 入力 -c copy -movflags +faststart 出力` で先頭に寄せられる（今回はしていない）。

### 動作確認（特商法ページの戻り先）
ライブサイトで、実際にリンクをたどって確認した。
- Roy's Channel のフッター → 特商法：`referrer` が `/roys-channel/`、名札は `roy-main` のまま、
  戻るリンクが `https://roys1105.github.io/roys-channel/` に切り替わった。
- シニアのトップ → 特商法：`referrer` がシニアのトップ、名札は `roy-senior`、
  戻るリンクは `index.html` のまま。

### 動作確認（デプロイ後のライブサイト）
- Roy's Channelへのボタンが出ていて、飛び先とタブ名が正しいことを確認。
- 集計の数字が出ることを確認（訪問 3回／39回、スマホ教室 0回／8回、脳いきいき手帳 0回／21回）。
  ※訪問の累計が103→39に減ったのは、8/14に「ライブぶんだけ数える」と決めた効果。
  手元での動作確認ぶんが除かれたため。減ったのではなく、数え方が正しくなった。
- 集計コードは、ページから取り出した実物を Node で2通り動かして確認した。
  totalsあり→全期間の合計を使う／totalsなし→これまでどおり results を足す。
- `image/cards/` の8枚は、ライブで直接開くと200で返る（curl・fetchとも確認）。
  **ブラウザ枠では `loading="lazy"` の画像が読み込まれない**（枠が非表示で描画されないため）。
  「8枚失敗」と出るが不具合ではない。lazy指定8枚だけが未読み込み、他8枚は全部OK、という
  切り分けで確認した。

### 動作確認（作業中）
- 年齢計算のロジックをNode.jsで日付を変えて検証：
  2026-08-15→61歳／11月4日→61歳／11月5日→62歳／2027-01-01→62歳／2027-11-05→63歳。
- `.roy-age` の書き換え処理を実ブラウザ上で実行し、2か所とも「61」に置き換わることを確認。
- ライブサイトのトップページを開き、「61歳、香川県三豊市在住。」と表示されているのを確認。

---

## 2026-08-14（金）

### やったこと
- **写真が入っていなかった8か所に、実際の写真を入れた。**
  これまで `<image-slot ... placeholder="○○の写真">` という空の枠のままだった箇所。
  - ④「こんな記事・動画があります」のカード6枚
  - ⑤「もう一つの顔」のカード2枚
- 画像は `image/cards/` を新設してそこに置いた（8枚・合計388KB）。
  カード用は 720×300、「もう一つの顔」用は 760×340 に切り出し、JPEG品質82で保存。
  作成スクリプトは scratchpad の `make_cards.py`。

| 場所 | 出力ファイル | 元の写真 |
|---|---|---|
| タブレットの選び方（YouTube） | card-tablet.jpg | `D:\Roy.S\Pictures\iPhone\20251231\IMG_0877.jpg` |
| スマホの困りごと解決（note） | card-sumaho.jpg | note素材「娘にスマホ操作を教えてもらっているシニア女性.png」 |
| パソコン周辺機器レビュー（YouTube） | card-pcgear.jpg | `D:\Roy.S\Pictures\iPhone\20250721\IMG_0023.JPG`（トラックボール） |
| 家族とのコミュニケーション（note） | card-kazoku.jpg | note素材「親子3世代でスマホレクチャー.png」 |
| ドローンを使ってみた（YouTube） | card-drone.jpg | `D:\Roy.S\Pictures\CANON EOS\20210720\IMG_1191.JPG` |
| 暮らしとお金の話（note） | card-kurashi.jpg | note素材「スマホでスイッチボットを…エアコンをつけている…jpg」 |
| バウンドテニス普及活動 | bound-tennis.jpg | `D:\Roy.S\Pictures\100CANON\20250505\IMG_3262.JPG`（会場全景） |
| バイク・キャンプ | hobby-bike.jpg | `D:\Roy.S\Pictures\100CANON\20201227\IMG_1992.JPG` |

- `<image-slot>` は使わず、素の `<img ... object-fit:cover; loading="lazy">` に置き換えた。
  未使用になった `image-slot` の `x-import` 行はそのまま残してある（消すと壊れる可能性があるため）。

- **「はじめてのスマホ教室」の紹介カードを、「脳いきいき手帳」カードと同じ横並びに揃えた。**
  これまでは幅520pxの縦積み（表紙が上・文章が下）で、すぐ下の脳いきいき手帳カードだけが
  横並びになっていて不ぞろいだった。
  - `max-width:520px; margin:0 auto; flex-direction:column` をやめ、
    脳いきいき手帳カードと同じ `display:flex; flex-wrap:wrap; gap:26px; align-items:center` に変更
  - 表紙画像を左カラム（`flex:1 1 240px`）に入れ、`max-width:308px`（＝脳いきいき手帳の
    スクショ2枚ぶんの幅と同じ）、角丸14px・枠線 `#D7E6EA` も同じにした
  - タグ／見出し／説明文／ボタンは右カラム（`flex:1 1 300px`）にまとめた
  - `flex-wrap` なので、スマホ幅では自動で「画像が上・文章が下」に折り返す（従来の見え方と同じ）

- **③自己紹介の実績カードを3枚にした。** 左から「訪問数」「はじめてのスマホ教室の視聴回数」
  「脳いきいき手帳の視聴回数」。
  - `materialPages` に `{ page: '/materials/nouikiiki-tebo.html', title: '脳いきいき手帳' }` を追加しただけ。
    カードは `getStatCards()` が `materialPages` を回して作るので、並び順もこの配列の順になる。
  - 集計側は元から動いていた。`material/nouikiiki-tebo.html` は前から
    `/materials/nouikiiki-tebo.html` として `track-page` に送っていて、数字も出ている（確認時 6回／21回）。
  - 3枚に増やしたら「脳いきいき手帳の視聴回数」が「…視聴回／数」と不自然に折り返したので、
    カードの左右パディングを 22px → 16px に詰めた。これで1行に収まる。

- **「note読者の声（あとで差し替え）」を「note読者の声」だけの表示にした。**
  枠の左上に小さめの見出し（13px・太字・`#55747C`）として置き、飾りの「」は外した。
  白い枠はそのまま残してあるので、実際の声が集まったらこの下に本文を足せばよい。
  - 最初は上下の余白を同じ（`padding:20px 22px`）にしていたら「左中央」に見えると指摘された。
    枠の高さは60pxしかないが、行の高さが広い（サイト全体の line-height を継承して18px→25px）ぶん
    余計に中央寄りに見えていた。`padding:14px 22px 26px` ＋ ラベルに `line-height:1.4` を指定して、
    上15px・下27px の配置に直した。さらに「もう少し上」と言われて上9pxまで詰めたが、
    枠自体が60pxしかないと詰めても効果が薄いので、**枠に高さを持たせる**方針に変えた。
  - 最終形：`padding:12px 22px 18px; min-height:120px; box-sizing:border-box;`
    → 枠 620×120px、ラベルは左23px・上13px、下に89pxの余白。
    この89pxが「あとで実際の読者の声を入れる場所」になる。
  - `box-sizing:border-box` を明示したのは、このページに全体のリセットCSSが無く、
    指定しないと min-height がパディングを含まない数字になってしまうため。

- **ページの上のほうに「更新履歴」欄を作った。** 場所はクロの「ようこそ」と教材紹介のあいだ。
  - データは template 内の `updates` 配列。**新しいお知らせは配列の一番上に足す。**
  - `getUpdateItems()` が `slice(0, 10)` するので、11件目より古いものは自動で表示から外れる。
  - 見た目は `.update-list` に `max-height:236px; overflow-y:auto`。1行44pxなので**5件ぶん**が
    ちょうど収まり、6件目以降はスクロール。6行目が16pxだけのぞくので「まだ続きがある」と分かる。
  - スクロールバーは薄い水色で常に見える色を当てた（`.update-list::-webkit-scrollbar` ほか）。
  - 520px以下では日付と本文を縦に積む（`.update-row{flex-direction:column}`）。
  - 初期の5件は、DEVLOGとgitログから実際にあった出来事を起こした（7/20公開、7/29脳いきいき手帳、
    7/30プラス版、7/31クロ、8/14写真）。**中身はロイさんが書き換える前提。**
- **フッターのクロを `kuro-bye.png` → `kuro.png`（おすわり全身）に差し替えた。**
  `kuro-bye.png` は片目をつぶった絵で、小さく表示すると**片目が無いように見える**とのこと。
  同じ絵を使っていた `material/` の3ページ（`.kuro-end`）もまとめて差し替えた。
  - `kuro.png` は縦長（620×880）で顔だけの絵（320×241）より縦に伸びるため、
    幅を トップ 104px → 96px、教材ページ 118px → 104px に控えめにした（表示 96×136px）。
  - **片目をつぶった絵は `kuro-bye.png` だけ。** 他の8枚は両目が開いている。
    今後クロの絵を選ぶときは `kuro-bye.png` を避ける。

- **薄い文字色をサイト全体で濃くした。** 更新履歴を2回「薄い」と言われたのがきっかけで、
  トップページ全体を調べ直した。ブラウザ上でコントラスト比を計算して洗い出している。

  | 場所 | 変更前 | 変更後 | コントラスト比 |
  |---|---|---|---|
  | 更新履歴の日付 | `#8FB0B8` 13px | `#2B4550` 14px 太字 | 2.28 → **10.15** |
  | 更新履歴の本文 | `#33505A` 14px | `#14262E` 15px | 8.62 → **15.6** |
  | 説明文・ラベル類（11か所） | `#55747C` | `#33505A` | 5.03 → 8.62 |
  | フッター | `#6E8790` | `#3F5A63` | 3.61 → 7.3 |
  | YouTubeタグ（3か所） | `#C1533F` | `#A33D28` | 4.04 → 6.1 |
  | 教材ページの「」（inaka-hitori-kasegu） | `#8FB0B8` | `#33505A` | 2.28 → 8.62 |

  - **色だけでなく字も大きくした**（更新履歴 13/14px → 14/15px）。
    「見辛い」は色より文字サイズの問題であることも多い。
  - 結果、**トップページの本文テキストは全部 WCAG AA（4.5:1）以上**になった
    （ブラウザで全要素を走査して0件を確認）。
  - 行が45pxになったので `.update-list` の max-height を 236px → 244px にした（5件ぶん）。
  - `material/nouikiiki-tebo.html` の `table.cmp td.no{color:#B9C6CA}` はとても薄いが、
    **その class を使っている場所が無い**（死んだCSS）ので触っていない。

- **アクセス数の水増しを止めた（重要）。** 訪問数に、手元での動作確認ぶんが全部混ざっていた。
  - 判明時点の内訳（累計103回）：ライブサイト `/roys-senior-it/` 28＋`/roys-senior-it/index.html` 6、
    特商法 2、**残り67回はローカル**（`/` 23、`/index.html` 28、
    `/C:/Users/roy_s/…/index.html` 13、`_backups/…` 3）。
    この日の作業中だけで 85→103 に増えていた。
  - **対策1（今後を止める）**：計測スクリプトの先頭に
    `var LIVE_HOSTS = ["roys1105.github.io"]; if (LIVE_HOSTS.indexOf(location.hostname) < 0) return;`
    を入れた。入れたファイルは5つ：`index.html`（バンドラー側の`<body>`直後のscript）、
    `tokushoho.html`、`material/` の3ページ。
  - **対策2（表示を絞る）**：`componentDidMount` の集計で、`type==='visit'` のうち
    `LIVE_VISIT_PAGES = ['/roys-senior-it/', '/roys-senior-it/index.html', '/tokushoho.html']`
    に入っているページだけ数えるようにした。→ 訪問数は 103回 → **36回** になった。
  - 教材ページの数（8回・21回）は、記録上ローカルぶんと区別が付かないのでそのまま。
    今後は対策1で混ざらない。
  - **独自ドメインにするときは、`LIVE_HOSTS`（5ファイル）と `LIVE_VISIT_PAGES`（template）の
    両方に足すこと。** 足し忘れると数が0になる。

### わかったこと・つまずいたこと
- **index.html の中身は「1行のJSON文字列」として埋め込まれている。**
  `<script type="__bundler/template">` の中身が JSON 文字列で、そこにページ本体のHTMLが丸ごと入っている。
  直接 Edit すると壊れるので、次の手順でしか触れない：
  1. JSONを取り出して `template.html` に展開する
  2. `template.html` を編集する
  3. JSONに戻して index.html に埋め直す（scratchpad の `repack.py`）
- **戻すときの注意が2つある。**
  - `</` は `<\/` にエスケープしないといけない（HTMLの `</script>` と衝突して壊れる）。
  - index.html は改行が CRLF。普通に読み書きすると LF に変わって全行が差分になるので、
    `io.open(..., newline='')` で読み書きする。
- **空の写真枠は index.html の8か所だけ**だった。material配下や nouikiiki 側には無い。
- バウンドテニスは大会の写真に他の方の顔が写るため、**人が写っていない会場全景**を選んだ。
  もっと動きのある写真にしたい場合は `100CANON\20250505` の IMG_3322 / IMG_3342 あたりが候補。
- **アクセス数の集計は、Worker の `/page-stats` が「最新500行」しか返さない**（`worker.js:168`）。
  1行＝(日付×種類×ページ)の組み合わせ。今35行・22日ぶんで、増え方は1日1.6行ほど。
  このままだと**10か月後くらいに500行を超え、古いぶんが累計から静かに抜け落ちる**。
  直すなら worker.js 側を `SELECT type, page, SUM(count) … GROUP BY type, page` にして
  合計を返す形にする（要 wrangler でのデプロイなので別作業）。
- `image-slot` の `x-import` を残したままなので、ページを開くたびに
  `.image-slots.state.json` への404が1回出る。動作に害はないが、消すなら x-import ごと。

### 次にやること
- **【デプロイと同時にやる約束】`/page-stats` を「合計を返す」形に直す。**
  ロイさんと 2026-08-14 に決めた。今のままだと500行を超えたときに累計が静かに落ちる。
  - worker.js 側：`results`（今日ぶん）は残したまま、
    `SELECT type, page, SUM(count) AS count FROM page_events GROUP BY type, page` の結果を
    `totals` として足す
  - index.html 側：累計は `totals` から、今日ぶんは `results` から取るように `componentDidMount` を直す
  - **両方を同時に反映しないと数字が壊れる**
  - リポジトリに wrangler.toml が無い（あるのは `material/worker.js` という控えだけ）ので、
    **実際のWorkerをどうやって更新しているかをロイさんに先に聞くこと**（Cloudflareの管理画面の可能性が高い）
- **この日の変更はまだ push していない。** 手元にあるだけ。次に開いたらここから再開する。
  変更済み（`git status`）：`index.html` / `tokushoho.html` / `material/` の3ページ / `DEVLOG.md`、
  新規：`image/cards/`（写真8枚）。
  **`git add material` とまとめてやらないこと。** `material/` には未追跡の
  `worker.js`（Workerの控え）と `反映手順.md`（作業メモ）が入っていて、一緒に上がってしまう。
  ファイルを明示して追加する：
  ```
  git add index.html tokushoho.html DEVLOG.md image/cards \
          material/hajimete-no-sumaho-kyoshitsu.html \
          material/inaka-hitori-kasegu.html \
          material/nouikiiki-tebo.html
  ```
  （最後に `git status` で、意図しないファイルが混ざっていないか見てから commit する）
- ロイさんが写真と見た目を確認 → OKならGitHubにpushしてライブサイトに反映する
- 各カードは今のところリンクなし。実際のnote記事・YouTube動画へのリンクを付けるかどうかは未定。

### 動作確認
- **文字色**：ブラウザ上で全テキスト要素のコントラスト比を計算し、
  4.5未満が**0件**になったことを確認（トップページ）。
- **更新履歴**：5件で表示、10件に増やすとスクロールが効き、完全に見えるのは5件、
  6件目が少しのぞくことを確認。
- **アクセス計測**：ローカル（localhost）で開いても `/track-page` への送信が
  1件も出ないことをネットワークログで確認。訪問数は 103回 → 36回。
- ローカルに簡易HTTPサーバを立てて `http://localhost:8777/index.html` を表示。
  8枚すべて読み込み成功を確認（naturalWidth が 720×300／760×340 で返る）。
- ④と⑤のセクションをスクリーンショットで目視確認。カード内に正しく収まっている。
- 教材紹介カードは 1100px幅（PC）と 420px幅（スマホ）の両方で目視確認。
  PCでは脳いきいき手帳カードと同じ横並び、スマホでは縦積みになることを確認した。
- 実績カード3枚と「note読者の声」もスクリーンショットで目視確認。
  パディングを詰めたあとの折り返しだけはプレビュー画面が固まって撮れなかったので、
  ラベルの高さを測って確認した（脳いきいき手帳＝19px＝1行、はじめてのスマホ教室＝43px＝2行）。
- ライブサイトへの反映は**未実施**。

---

## 2026-07-31（金）

### やったこと
- **黒猫「クロ」をサイト全体に入れた。** これまでクロが出ていたのは
  `nouikiiki/` `nouikiiki-plus/` と `material/nouikiiki-tebo.html` の画面写真だけだった。
  - `image/kuro/` を新設。**まっ黒に直した透過PNGを9枚**置いた
    （`kuro.png` ＝おすわり全身／`kuro-hello` `kuro-go` `kuro-bye` `kuro-welcome`
    `kuro-night` `kuro-morning` `kuro-home` `kuro-send` ＝場面別）
  - `index.html`（トップ）… ①と①.5のあいだに「ようこそ！ぼくはクロ」、
    お問い合わせの手前に「わからないことがあったら、いつでも聞いてね」、
    フッターの上に見送りのクロ
  - `material/hajimete-no-sumaho-kyoshitsu.html` … 冒頭「あわてなくて大丈夫」、
    練習チェックリストの手前「ここまでおつかれさま」、末尾の🌱をクロに差し替え
  - `material/inaka-hitori-kasegu.html` … 冒頭「ようこそ！」、Q&Aの手前「あとは、やってみるだけ！」、
    末尾の🌱をクロに差し替え
  - `material/nouikiiki-tebo.html` … 冒頭「ぼくがクロだよ」、
    「黒猫のクロが相手」カードの絵文字🐈‍⬛を**本物のクロの絵**に、
    注意書きの手前「むりをしない日があってもいいんだよ」、末尾もクロに
  - 吹き出しは全ページ共通の `.kuro-say`（白地・紺の枠・16px太字）。スマホでは絵を74pxに縮める

- **スマホでトップページが横にはみ出す不具合を直した**（7/29から持ち越していた件）。
  - ヘッダーロゴ … 画像に `class="site-logo"` を付け、
    640px以下で `height:auto !important; max-width:240px`、400px以下で `max-width:196px`。
    実寸372pxのまま並んでいたのが原因（375px幅で scrollWidth 436px ＝ 61pxはみ出し）
  - 「もう一つの顔」のカード … `minmax(300px,1fr)` を **`minmax(min(300px,100%),1fr)`** に。
    320px幅のときだけ4pxはみ出していた

### わかったこと・つまずいたこと
- **クロの絵の出どころ**：`Pictures\可愛い黒猫の日常\可愛い黒猫の日常１`（8枚）。
  同じフォルダの `可愛い黒猫の挨拶`（25枚）は毛がふさふさの**別の絵柄**なので混ぜない。
  スタンプ絵には上に文字が入っているので、**行ごとの不透明ピクセル数がいちばん減る行**で
  切り落として文字を消している（作業スクリプトは scratchpad の `build_assets.py`）。
- **毛色は元がこげ茶。** `nouikiiki-app/クロの画像を作りなおす.py` の `to_black()` を
  そのまま流用して黒に寄せた（`darken=0.16`）。目の琥珀色・耳のピンクは残る。
- **bundler の書き戻しでハマった点**：`__bundler/template` の中身は
  **引用符を含んだJSON文字列**なので、`json.dumps` した結果から引用符を外してはいけない。
  外して書き戻すと次に `json.loads` できなくなる（一度壊してバックアップから戻した）。
  正しい手順：`json.loads` → 編集 → `json.dumps(ensure_ascii=False)` →
  `</` を `<\/` に戻す → **そのまま** group(2) の位置に差し込む → 読み直して往復一致を確認。

- **`minmax(300px,1fr)` は狭い画面で縮まない。** カードの最小幅より画面が狭いと、
  グリッドはその最小幅を守ろうとしてはみ出す。`min(300px,100%)` で包めば、
  狭いときだけ画面幅まで縮んでくれる。**auto-fit のグリッドを書くときは毎回これ。**

### 次にやること
- クロのセリフは今は固定文。将来ページごとに増やすなら `.kuro-say` を足すだけでよい
- `kuro-home.png` は左上に文字の消し残しが少しあるので、使うときは切り直す

### 動作確認
- ローカル（http://localhost:8936）で確認
  - トップ … 画像9枚すべて表示、`.kuro-say` 2か所＋フッターのクロ1つ、横スクロールなし
  - 教材3ページ … いずれも画像の読み込み失敗0件、`.kuro-say` 2か所＋末尾のクロ1つ
  - 幅375px（スマホ）… 3ページとも横スクロールなし（比較表の横スクロールは元からの仕様）
  - 「黒猫のクロが相手」カードのアイコン … 52pxの枠に32×46pxで収まっている
  - 横はみ出しの確認（トップ）… 320 / 375 / 414 / 640 / 1280px すべて
    scrollWidth = clientWidth。ロゴは 196×44 → 240×54 → 372×84（パソコンは元のまま）
  - **ブラウザ枠が表示できずスクリーンショットは撮れていない**（数値での確認のみ）

---

## 2026-07-30（木）

### やったこと
- **脳いきいき手帳を「無料版」と「プラス版（有料）」に分けた**
  - `nouikiiki/` … **無料版 v0.1 に差し替え**（昨日まではフル機能版を置いていた。あれはテスト公開）
  - `nouikiiki-plus/` … **プラス版（v0.3.7）を新設**。開くと合言葉の入力画面が出る
    （無料版は v0.1.2。どちらもクロの画像を入れたぶんだけ番号を上げている）
  - `material/nouikiiki-tebo.html` … **2部構成に全面改稿**。前半＝無料版の紹介、
    後半（`#plus`）＝プラス版の案内（買い切り1,000円・比較表10項目・画面5枚・合言葉の説明）。
    お申し込みは「準備中」とし、お問い合わせへ誘導している
  - `image/nouikiiki/` … 無料版の画面3枚（`free-*.png`）と合言葉画面（`plus-gate.png`）を追加
  - `index.html` … トップのカードを無料版の内容に書き換え（画像も無料版の画面に差し替え）

- **黒猫「クロ」を、ロイさんのイラスト（LINEスタンプ用の透過PNG）に差し替えた**。
  毛色のこげ茶は黒に置きかえ済み。アプリ2つのマスコットとアイコン3種、
  紹介ページの画像（`free-home.png` / `plus-gate.png`）も差し替え・撮り直し済み。
  作り直す手順は `dev/tool/nouikiiki-app/クロの画像を作りなおす.py`

### わかったこと・つまずいたこと
- **同じサイトに2つのアプリを置くときは、キャッシュ名の接頭辞を分けること。**
  サービスワーカーの「自分以外のキャッシュを消す」処理が、お互いを消し合ってしまう。
  → `nouikiiki-free-` と `nouikiiki-plus-` に分けて、自分の接頭辞だけ消すように両方直した。
- **記録（localStorage）はサイト全体で共通**なので、無料版でつけた記録はプラス版に引き継がれる。
  これは「乗り換えても続きから使えます」という売り文句にできる。
- 合言葉方式は、プログラムを読める人には回避できる（`nouikiiki-plus/js/gate.js` は誰でも見られる）。
  1,000円・シニア向けという条件で分かりやすさを優先した判断。詳しくは
  `dev/tool/nouikiiki-app/合言葉のかえかた.md`。

- **GitHub に push して公開した**（コミット cbcf81e、Pages ビルド success）

### 次にやること
- 決済方法を決めて、紹介ページの「お申し込み受付：準備中」を差し替える
- **売り始める前に `tokushoho.html` にアプリの販売条件（価格・支払方法・返品）を追記する**
- ヘッダーロゴの横はみ出しを直す（前回からの持ち越し）

### 動作確認
- ローカル（http://localhost:8936）で確認
  - `/nouikiiki/` … 無料版（14画面）、ゲートなし、プラス版への案内リンクOK
  - `/nouikiiki-plus/` … ゲートが出る。ちがう合言葉は弾き、正しい合言葉（英数字・かな両方）で開く。
    解錠後はリロードしても聞かれない。41画面・ゲーム開始までJSエラー0件
  - 2つのサービスワーカーとキャッシュが**共存**することを確認
  - 紹介ページ … 画像9枚すべて表示、リンク先4種すべて正常、比較表10行
  - トップページ … カードの文言・画像とも無料版のものに更新済み
- 本番（https://roys1105.github.io/roys-senior-it/ ）でも確認
  - `/nouikiiki/` … 無料版14画面、ゲートなし、クロの画像2枚とも表示
  - `/nouikiiki-plus/` … ゲートが出る／合言葉で開く／サービスワーカーの scope もOK
  - キャッシュは `nouikiiki-plus-0.3.7` と `nouikiiki-free-0.1.2` が**共存**。JSエラー0件
  - 主要URLはすべて HTTP 200

---

## 2026-07-29（水）

### やったこと
- **脳いきいき手帳（アプリ）をサイトから使えるようにした**
  - `nouikiiki/` … アプリ本体一式（`dev/tool/nouikiiki-app` からコピー。原本はあちら側）
  - `material/nouikiiki-tebo.html` … **紹介ページを新規作成**。実際の画面6枚＋解説、
    「脳いきいき手帳をひらく」ボタンで `../nouikiiki/` を**別タブ**で開く。
    ホーム画面への追加手順（iPhone / Android / パソコン）と注意書きも掲載
  - `image/nouikiiki/*.png` … 紹介ページ用スクリーンショット6枚
  - `index.html` … 教材カードの下に**アプリ紹介カード**（横いっぱい）を追加。
    ヘッダーのナビ（パソコン用・スマホ用の両方）に「脳トレアプリ」を追加

### わかったこと・つまずいたこと
- **index.html は bundler 形式で、ページ本体は最終行の JSON 文字列（`__bundler/template`）の中にある。**
  直接エディタで書き換えず、Python で `json.loads` → 文字列を編集 → `json.dumps(ensure_ascii=False)` →
  **`</` を `<\/` に戻す**（元ファイルと同じエスケープ流儀）→ 書き戻し、が安全。
  書き戻したあと必ず読み直してデコードが一致するか確認する（今回は round-trip OK を確認）。
- **挿入位置の目印は、インデントまで含めて指定する。** 最初 `"      </div>\n"`（6字下げ）で探したら、
  8字下げの `        </div>` にも部分一致してしまい、カードの**内側**に入ってしまった。
  → 先頭に改行を付けた `"\n      </div>\n"` で解決。
- **アプリは iframe 埋め込みではなく「別ページで起動」にした。** iframe だと
  「ホーム画面に追加」ができず、アプリとして使えなくなるため。
- **既知の不具合（今回の変更とは無関係）**：横幅390px程度のスマホでトップページが
  **横にはみ出す**（`scrollWidth` 436px）。原因はヘッダーのロゴ画像が `height:84px; width:auto` で
  372px あり、ハンバーガーボタンと並ぶと入りきらないため。変更前のファイルでも同じ。

- **GitHub に push して公開した**（コミット `d23e525`、Pages ビルド success）

### 次にやること
- ヘッダーロゴの横はみ出しを直す（スマホでは少し小さくする）
- アプリを更新したときは `dev/tool/nouikiiki-app/サイトへ反映.bat` を実行 → その後 push

### 動作確認
- ローカルサーバー（http://localhost:8936）で確認
  - `/nouikiiki/` … JSエラー0件、サービスワーカーの scope は `/nouikiiki/`、manifest も解決OK
  - `/material/nouikiiki-tebo.html` … 画像6枚すべて表示、幅390pxでもはみ出しなし、起動ボタン 289×71px
  - `/index.html` … 紹介カードが教材カードの下に横いっぱいで表示、リンク先 `material/nouikiiki-tebo.html` OK
- 本番（https://roys1105.github.io/roys-senior-it/ ）でも確認
  - トップの紹介カード … 画像2枚とも表示、リンク先OK、ナビに「脳トレアプリ」あり
  - `/nouikiiki/` … 起動OK、JSエラー0件、サービスワーカー scope `/roys-senior-it/nouikiiki/`
  - 主要URLはすべて HTTP 200（紹介ページ・アプリ・js・画像・manifest）
